# ==============================================
# CONFIGURACIÓN DOCKER - CAFÉS MARLOY
# ==============================================
# Copia este archivo como docker.env para usar con Docker

# Configuración MySQL en Docker
DB_HOST=localhost
DB_PORT=3306
DB_NAME=Obligatorio
DB_USER=root
DB_PASSWORD=rootroot

# Configuración del Backend
FLASK_PORT=5000
FLASK_DEBUG=true
FLASK_HOST=0.0.0.0

# JWT y CORS
JWT_SECRET_KEY=cafes-marloy-secret-key-2025
FRONTEND_URLS=http://localhost:3000,http://localhost:3001,http://localhost:3002

# ==============================================
# INSTRUCCIONES DOCKER:
# ==============================================
# 1. Instala Docker Desktop: https://www.docker.com/products/docker-desktop
# 2. Ejecuta: docker-compose up -d
# 3. Espera que la BD esté lista (30-60 segundos)
# 4. Inicia el backend: ./start_backend.sh
# 5. Adminer disponible en: http://localhost:8080
#    Servidor: mysql, Usuario: root, Contraseña: rootroot 